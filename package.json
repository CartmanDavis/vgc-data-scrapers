{
  "name": "vgc-usage-stats",
  "private": true,
  "workspaces": [
    "common",
    "cli",
    "docs"
  ],
  "scripts": {
    "build": "tsc --build --verbose",
    "clean": "tsc --build --clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy:functions": "cp supabase/functions.sql supabase/migrations/$(date +%Y%m%d%H%M%S)_functions.sql && supabase db push"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
