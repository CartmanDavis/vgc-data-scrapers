{
  "name": "vgc-usage-stats",
  "private": true,
  "workspaces": [
    "common",
    "cli",
    "frontend"
  ],
  "scripts": {
    "build": "tsc --build --verbose",
    "clean": "tsc --build --clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
