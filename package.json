{
  "name": "vgc-usage-stats",
  "private": true,
  "workspaces": [
    "common",
    "cli"
  ],
  "scripts": {
    "build": "npm run build -w @vgc/common && npm run build -w @vgc/cli",
    "clean": "npm run clean -w @vgc/common && npm run clean -w @vgc/cli",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
